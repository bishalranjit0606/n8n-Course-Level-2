# n8n-Course-Level-2
Use the data structure of n8n correctly. Process different data types (for example, XML, HTML, date, time, and binary data). Merge data from different sources (for example, a database, spreadsheet, or CRM). Use functions and JavaScript code in the Code node. Deal with error workflows and workflow errors.
